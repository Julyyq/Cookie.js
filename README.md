Cookie.js
=========

Operate cookie use plain javascript

Usage
=========

    Cookie.set(name, value, expires);
    
    Cookie.get(name);
    
    Cookie.remove(name);
    
Example
==========

    Cookie.set("name", "Allen", 1);
    
    Cookie.get("name");
    
    //output: "Allen"
